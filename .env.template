DATABASE_NAME=kubstu_docs_db
DATABASE_USER=sysadmin
DATABASE_PASSWORD=changeme
DATABASE_HOST=db
DATABASE_PORT=5432

SECRET_KEY='change-me-to-a-long-random-string'
DEBUG=False

CELERY_BROKER_URL=redis://redis:6379/2
CELERY_RESULT_BACKEND=redis://redis:6379/3

EMAIL_HOST_USER=your@yandex.ru
EMAIL_HOST_PASSWORD=your-app-password

# Cache for the push-notification 15-minute throttle (notifications.tasks).
CACHE_REDIS_URL=redis://redis:6379/4

# Web Push (VAPID). Generate with `python manage.py generate_vapid_keys`
# (see notifications/management/commands/) and paste the output here.
# VAPID_CLAIM_SUB must be a mailto: or https: URI identifying the sender.
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_CLAIM_SUB=mailto:admin@example.com
